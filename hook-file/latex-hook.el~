;;;KeyBindings
(add-hook 'org-mode-hook
          '(lambda ()
             (local-set-key (kbd "C-i") 'new)
             (local-set-key (kbd "C-c C-c") 'comment-region)
             (local-set-key (kbd "C-c C-u") 'uncomment-region)
             )
          )


(provide 'org-hook)
